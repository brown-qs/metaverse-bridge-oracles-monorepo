import { Theme } from '@mui/material';

export const styles = (theme: Theme) => ({
    menuNavItemDrawer: {
        marginTop: '4px',
        marginBottom: '4px' 
    },
    menuNavItemRegular: {
        marginLeft: '4px',
        marginRight: '4px' 
    }
})
