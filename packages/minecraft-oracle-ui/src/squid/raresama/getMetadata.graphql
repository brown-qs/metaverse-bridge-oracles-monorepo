query getMetadata($where: TokenWhereInput) {
  tokens(where: $where, limit: 1000) {
    metadata {
      type
      name
      layers
      image
      description
      composite
      attributes {
        value
        traitType
        displayType
      }
    }
    numericId
    contract {
      id
    }
  }
}
